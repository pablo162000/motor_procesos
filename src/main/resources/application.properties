spring.application.name=motor-procesos

#Puerto
server.port=${SERVER_PORT:5041}
server.servlet.context-path=/API/tesis

#Rutas de consumo
correo.url=${CORREO_URL:http://correo:8080/API/tesis/}
corsorigin.url=${CORSORIGIN_URL:http://tesis.local}


#Coneccion Base
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://dpg-d0qhmemmcj7s73e22t0g-a.oregon-postgres.render.com:5432/pruebatesis}
spring.datasource.username=${POSTGRES_USER:pruebatesis_user}
spring.datasource.password=${POSTGRES_PASSWORD:jikL7PgTZxdCjciyyt5xuV35uZFNxKEa}
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.liquibase.enabled=false

# Habilitar balanceo de carga
spring.cloud.discovery.enabled=false

#Motor de procesos
flowable.rest.enabled=true
flowable.actuator.enabled=true

#Actuator
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.info.env.enabled=true
management.info.build.enabled=true

